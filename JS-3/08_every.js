//------------------Every---------------------------------

// Return true if every element of array gives true for some function. Else returns false.

// arr.every(some sunction defination or name);

let arr = [1, 5, 3, 7];

let even = arr.every((el) => (el%2 ==0)); // false because element are odd or function is even number given